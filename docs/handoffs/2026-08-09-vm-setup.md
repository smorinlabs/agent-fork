# Session handoff — agent-fork VM setup & readiness — 2026-08-09

## 🎯 Outcome
**Goal:** bring the `agent-fork` Lima VM to implementation-ready: guest `flox` installed via `project sync`, repo at the post-merge `main`, `project check` green, agent CLIs + auth re-verified. Ends when the checklist in *Done means* below is fully green.
**Out of scope:** any implementation or test-writing (that is `docs/handoffs/2026-08-09-vm-implementation.md`, run *inside* the VM afterwards); Phase E/F release work (host-bound, later).
**Runs on:** the **host Mac** (this machine) — `limactl`/`sandboxctl` are host commands.
**Self-contained:** ✓ stands alone — the prior sandbox handoff (`docs/handoffs/2026-07-26-sandbox-continuation-v2.md`) is **uncommitted in the main checkout and does not travel**; everything load-bearing from it is inlined below.

## ⚠ Portability & dependency preflight — read first
- This branch (`docs/test-architecture-spec`): clean, fully pushed. This handoff travels once the PR merges.
- ⚠ `docs/handoffs/2026-07-26-sandbox-continuation-v2.md` — uncommitted, host-only; **do not depend on it** (essence inlined here).
- ⚠ The VM itself exists **only on this Mac** (`~/.lima/agent-fork`). On any other machine, use the full bring-up recipe at the bottom.
- ⚠ Auth state (Codex/Claude/gh in guest) last verified **2026-07-26** — assume stale until `auth-check` passes again.

## 🧭 Where you are
- Repo: `agent-fork` · origin `https://github.com/smorinlabs/agent-fork.git` · default `main`
- Host checkout (this machine): `/Users/stevemorin/c/agent-fork` (keep clean; work happened in worktree `agent-fork-test-arch`)
- PR: `docs/test-architecture-spec` → `main` (26+ commits: test-architecture spec, corpus amendments A1–A14, 190-row TEST-MATRIX, 185 stubs, check-matrix drift guard). **Guest repo sync below requires this PR merged.**
- VM (verified 2026-08-09): `agent-fork` · Lima/VZ · Ubuntu 24.04 ARM64 · **Running** · no host mounts · OAuth forward port **1456** (1455 belongs to the unrelated `skillsmith-p17` VM — never confuse them; always pass `SANDBOX_VM_NAME=agent-fork` explicitly)
- Guest state (prepared 2026-07-23..26): toolchain converged (Node 18.19.1, gh 2.45.0, Python, jq, ripgrep) · Codex CLI **0.145.0** (passes REQ-27: fork ≥0.81.0, env ≥0.95.0) · Claude Code **2.1.218** (passes ≥2.0.73) · **git 2.43.0** (meets `TEST_HARNESS_GIT_MIN = 2.43`, re-verified 2026-08-09) · 40 host skills imported · project definition imported · repo cloned at `/work/agent-fork` @ `9a907b1` (**stale — pre-merge**)
- **The one standing blocker:** guest `flox` was never installed — `project sync agent-fork` (which installs it, then runs `flox activate -- uv sync`) has never completed. `project check` fails with `bash: flox: command not found` until it does.

## 📎 Artifacts & sources of truth
| What | Repo-relative path | Status |
|---|---|---|
| sandboxctl wrapper | *(host skill, not in repo)* `/Users/stevemorin/.claude/skills/sandbox-lima/scripts/sandboxctl` | host-machine convenience; other machines: `sandbox-lima` skill |
| Project definition (guest-local, inlined below) | — | ✓ inlined |
| Implementation handoff (next step after this one) | `docs/handoffs/2026-08-09-vm-implementation.md` | ✓ committed beside this file |

## 📋 Plan · inlined skeleton
All host-side; `S="SANDBOX_VM_NAME=agent-fork SANDBOX_OAUTH_PORT=1456"` and `sandboxctl` = the wrapper above.

1. **VM up + isolation:** `limactl start agent-fork` (if not Running) → `env $S sandboxctl check` (expect PASS, no host mounts).
2. **Auth re-verify:** `env $S sandboxctl auth-check`. On any failure → `env $S sandboxctl auth` (interactive: Codex ChatGPT device auth, Claude login, `gh auth login --web` + `gh auth setup-git`). Known gotcha: `gh auth login --web` can fail with `GraphQL: API rate limit already exceeded` purely from account-level quota — check `gh api rate_limit --jq '.resources.graphql'` (REST, quota-free) and retry after the hourly reset rather than assuming auth is broken.
3. **Sync the project (installs flox — the blocker):** run **from the host** (the wrapper drives the guest itself; the host path does not exist inside the no-mounts VM): `env $S sandboxctl project sync agent-fork` — runs the setup command below (flox aarch64 deb → `flox activate -- uv sync`) and ff-pulls the repo. **Run after the PR merges** so `/work/agent-fork` lands on the post-merge `main`; verify with `git -C /work/agent-fork log --oneline -1` (expect the merge commit, not `9a907b1`).
4. **Green gate:** `env $S sandboxctl project check agent-fork` — expect `make check` ✓ and `just all` ✓ (post-merge: **28 passed, 185 skipped**, zero warnings) · also `just check-matrix` exit 0 inside `/work/agent-fork`.
5. **Version floor sanity in guest:** `git --version` ≥ 2.43 ✓ · `claude --version` ≥ 2.0.73 ✓ · `codex --version` ≥ 0.95 ✓.

**Done means:** steps 1–5 all green. Then start the implementation session *inside* the VM per `docs/handoffs/2026-08-09-vm-implementation.md`.

### Project definition (guest-local file; recreate with `sandboxctl project import agent-fork <file>`)
```toml
version = 1
id = "agent-fork"
primary_repo = "agent-fork"
required_agents = ["codex", "claude"]

[[repos]]
name = "agent-fork"
url = "https://github.com/smorinlabs/agent-fork.git"
path = "/work/agent-fork"
branch = "main"
update = "ff-only"

[commands]
setup = "command -v flox >/dev/null || { curl -fsSLo /tmp/flox.deb https://downloads.flox.dev/by-env/stable/deb/flox-1.13.2.aarch64-linux.deb && sudo apt-get install -y /tmp/flox.deb; }; flox activate -- uv sync"
check = ["flox activate -- make check", "flox activate -- just all"]
goal = "Read IMPLEMENTATION-PROMPT.md and docs/handoffs/2026-08-09-vm-implementation.md at the repo root, then follow the implementation handoff."
```

> ⚠ **Integrity note:** the setup command above is quoted verbatim from the already-deployed guest-side definition; its fallback fetches the Flox `.deb` over HTTPS **without checksum pinning** and installs it as root. Hardening (pinned checksum, or Flox's signed apt repository) is tracked for the `sandbox-lima` skill's setup-command revision — when recreating from scratch on a new machine, prefer Flox's signed-repo install instructions over this fallback.

### Full bring-up recipe (only if the VM is missing — e.g. a different machine)
Skills: `sandbox-lima` → `sandbox-prepare` → `sandbox-project`. (1) `env $S sandboxctl setup` then `check`; (2) `env $S SANDBOX_REQUIRED_AGENTS=codex,claude sandboxctl prepare`; (3) `sandboxctl auth` (interactive, human) → `auth-check`; (4) `sandboxctl import-skills <staged-dir>` — stage a clean symlink farm first (the packer hard-fails on the first entry missing `SKILL.md`); (5) `project import` with the TOML above; (6) `project sync`; (7) `project check`; (8) `project shell`.

## 🧠 Critical context that won't survive a fresh window
- **Why the VM:** owner decision — all implementation and test execution happens in the VM while the pipeline code is untrusted (tests manipulate real git repos, signals, processes). Doc/skeleton work stayed on the host; red-green starts in the guest.
- The VM was seen **Broken** on 2026-07-27 and recovered to a normal Stopped/Running state by 2026-08-08 without intervention — if it shows Broken again, `limactl stop -f agent-fork && limactl start agent-fork` before reaching for a rebuild.
- No host mounts is deliberate (isolation): code moves **only via GitHub** (guest has gh auth).
- `TEST_HARNESS_GIT_MIN = 2.43` hard-errors (never skips) for F/C/R tiers — guest 2.43.0 is exactly at the floor, by design (no pin; verified no behavioral drift vs host 2.50).

## 👉 First action
`limactl list | grep agent-fork` — if Running, go straight to step 2 (auth re-verify).

## ℹ How this was made
Composed live from the 2026-08-08/09 planning+skeleton session (no digest run — full context in-session) · machine: host Mac (darwin) · self-contained: ✓ (VM state re-probed live at write time).
