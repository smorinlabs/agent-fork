interface:
  display_name: "Agent Fork"
  short_description: "Inspect, print a session command, or fork"
  default_prompt: "Use $agent-fork --session to inspect with a fork command, $agent-fork --session-only to print only that command, or $agent-fork to create a verified fork."
